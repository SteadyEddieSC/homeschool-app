# Synthetic Fixtures

These files describe repeatable public-demo scenarios and intentionally contain no real family information.

The current fixture schema is a test-design contract, not yet the application's direct import/export schema. A future data adapter will convert these scenarios into the actual versioned state format and add round-trip tests before fixtures are exposed through the UI.
