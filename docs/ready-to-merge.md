# Ready to Merge

The repository foundation is ready to be reviewed as one squash-merged pull request. Full browser CI will activate automatically after the sanitized v10.32 HTML is added at the release manifest path.
