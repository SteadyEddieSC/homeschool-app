# Repository Status

Foundation branch is ready for pull request and squash merge. The release artifact bootstrap is tracked separately because the connector cannot safely upload the 5.1 MB HTML as one text payload.
