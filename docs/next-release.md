# Next Release Focus

The next implementation release should complete the one-time sanitized artifact bootstrap, activate browser CI, run a real full-app mobile regression, add demo reset/load behavior, and prepare the Cloudflare Pages preview. Source extraction should wait until those protections are operating.
