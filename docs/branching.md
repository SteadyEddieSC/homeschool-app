# Branching and Merge Policy

- `main` is the deployable baseline.
- Use short-lived `agent/`, `fix/`, `feat/`, and `docs/` branches.
- Open a pull request for normal changes.
- Prefer squash merge so each release or change lands as one intentional commit.
- Do not force-push `main`.
- Repository administration and destructive settings remain explicit owner actions.
